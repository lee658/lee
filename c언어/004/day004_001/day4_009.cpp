#include <stdio.h>

int main() {
	char x = 'A' ;
	printf("%d, %d, %d\n", x, x + 1, x + 2);
	printf("%c, %c %c", x, x + 1, x + 2);
	
	/*int id, pass;
	scanf_s("%d", &id);
	scanf_s("%d", &pass);
	printf("%d %d",id, pass);*/


	return 0;
}