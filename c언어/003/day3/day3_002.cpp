#include <stdio.h>

int main(void) {
	int x, y, sum, di, mu, div, te;

	x = 4;
	y = 8;

	sum = x + y;
	di = x - y;
	mu = x * y;
	div = x / y;
	printf("%d, %d, %d, %d", sum, di, mu, div);
	


	return 0;
}