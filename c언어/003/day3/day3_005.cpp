#include <stdio.h>

int main() {
	/*double a, b;
	int c;
	scanf_s("%d", &a);
	scanf_s("%d", &b);
	c = b / a;
	printf("%d", c);
	*/

	/*double w, h, area, total;
	w = 10.0;
	h = 5.0;
	area = w * h;
	total = w * 2 + h * 2;
	printf("%lf,\n %lf", area, total);
	*/















	return 0;
}